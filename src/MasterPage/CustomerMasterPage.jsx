import { Typography } from '@mui/material'

function CustomerMasterPage() {
  return (
    <div>
      <Typography variant="h4" className="page-title">Customer Master</Typography>
      <Typography variant="body1" className="page-subtitle">
        Manage customer entities, contracts, and billing profile references here.
      </Typography>
    </div>
  )
}

export default CustomerMasterPage
