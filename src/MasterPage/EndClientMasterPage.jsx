import { Typography } from '@mui/material'

function EndClientMasterPage() {
  return (
    <div>
      <Typography variant="h4" className="page-title">End-Client Master</Typography>
      <Typography variant="body1" className="page-subtitle">
        Manage end-client details, contact points, and engagement metadata here.
      </Typography>
    </div>
  )
}

export default EndClientMasterPage
