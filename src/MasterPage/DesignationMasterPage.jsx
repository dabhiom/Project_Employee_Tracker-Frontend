import { Typography } from '@mui/material'

function DesignationMasterPage() {
  return (
    <div>
      <Typography variant="h4" className="page-title">Designation Master</Typography>
      <Typography variant="body1" className="page-subtitle">
        Manage designation records, hierarchy mapping, and role alignment here.
      </Typography>
    </div>
  )
}

export default DesignationMasterPage
