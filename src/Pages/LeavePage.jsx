import { Typography } from '@mui/material'

function LeavePage() {
  return (
    <div>
      <Typography variant="h4" className="page-title">Leave</Typography>
      <Typography variant="body1" className="page-subtitle">
        Leave tracking layout is prepared. Add calendar and approval workflow next.
      </Typography>
    </div>
  )
}

export default LeavePage
