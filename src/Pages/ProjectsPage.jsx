import { Typography } from '@mui/material'

function ProjectsPage() {
  return (
    <div>
      <Typography variant="h4" className="page-title">Projects</Typography>
      <Typography variant="body1" className="page-subtitle">
        Project management layout is ready for list, timeline, and assignment modules.
      </Typography>
    </div>
  )
}

export default ProjectsPage
