import { Typography } from '@mui/material'

function ResourcePage() {
  return (
    <div>
      <Typography variant="h4" className="page-title">Resource</Typography>
      <Typography variant="body1" className="page-subtitle">
        Resource page layout is ready. Add documents, guides, and links here.
      </Typography>
    </div>
  )
}

export default ResourcePage
