import { Typography } from '@mui/material'

function EmployeesPage() {
  return (
    <div>
      <Typography variant="h4" className="page-title">Employees</Typography>
      <Typography variant="body1" className="page-subtitle">
        Employee management page layout is ready. Add table/form features here.
      </Typography>
    </div>
  )
}

export default EmployeesPage
